const header = ({title}) => {
  return (
    <header style={{padding:"0.5 rem", backgroundColor:"blue", color:"white", textAlign:"center"}}>
      <h1>{title}</h1>
    </header>
  )
}

export default header